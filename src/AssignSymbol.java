
public class AssignSymbol {

	public String assign(String input)
	{
		//Should I be trying to have the input as the data from a text file that would be better
		//and split it at first by words then go through every word and find out which is needed? 
		//GIVE THIS A GO
		
		String [] wordList = input.split(" "); 
		String symbolList = (""); 
		
		
	}
}
